Module Life_Long_Planning_A_Star
    Sub LFAStar(maze As List(Of Node), showPath As Boolean, showSolveTime As Boolean, delay As Integer, solvingColour As ConsoleColor)
        Dim start = GetStart(maze)
        Dim target = GetGoal(maze)
        Dim queue As New PriorityQueue(Of Node)

        While True

        End While


    End Sub

    Sub computeShortestPath(queue As PriorityQueue(Of Node))

    End Sub

End Module
